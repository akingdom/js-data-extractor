# js-data-extractor
Pure‑Python JavaScript literal extractor – extracts object/array assignments from JS files without regex or eval. Handles comments, unquoted keys, trailing commas, hex/octal/binary numbers, NaN, Infinity, and more.
